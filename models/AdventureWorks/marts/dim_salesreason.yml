version: 2

models:
  - name: dim_salesreason
    columns:
      - name: salesorderid
        description: primary key of dim_salesreason
        tests:
          - unique
          - not_null

      - name: Reasons
        description: all reason for a sale concatenated